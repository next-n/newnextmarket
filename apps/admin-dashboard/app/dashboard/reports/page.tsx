import { PagePlaceholder } from "@/components/common/page-placeholder";

export default function ReportsPage() {
  return (
    <PagePlaceholder
      title="Reports"
      description="Revenue, order, product, and customer reports will connect to backend report APIs."
    />
  );
}
